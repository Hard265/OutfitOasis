import { Text, View } from "react-native";

export default function ForgotPasswordPage() {
    return (
        <View className="flex-1 justify-center items-center bg-white dark:bg-black">
            <Text className="dark:text-white">Recover account</Text>
        </View>
    )
}